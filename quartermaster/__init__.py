"""Quartermaster — a deterministic control plane that drives the Claude CLI over
issue-tracker tickets, with a Secrets Broker that holds all external credentials
so the model never sees a raw key.

See the docs in the repo root (README.md, ARCHITECTURE.md, SECURITY.md)."""

__version__ = "0.1.0"
